import React from 'react';
import styles from '../../styles/settings.module.scss';

const Settings = () => {
	return(
	<div id={styles.settingsContainer}><h1>Welcome to the settings page!</h1></div>
	)
}

export default Settings;
